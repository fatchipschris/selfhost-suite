# SilverBullet notes — KEYS ONLY.
# SB_USER is "user:password" for HTTP basic auth (SilverBullet has no multiuser
# accounts). deploy.sh generates the password and writes user=ADMIN_EMAIL local
# part. Format: SB_USER=<username>:<password>
SB_USER=__GENERATE_BASIC_AUTH__
